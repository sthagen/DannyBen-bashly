= view_marker

> printf "%s\n" "{{ strings[:arguments].color(:caption) }}"

if args.any?
  >
  args.each do |arg|
    = arg.render(:usage)
  end
end

if catch_all.help
  >
  > echo "  {{ catch_all.label }}"
  > printf "{{ user_string catch_all.help, indent: 4 }}\n"
  > echo
end

>

