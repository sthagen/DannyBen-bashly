= view_marker

> {{ positive_aliases.join " | " }})
= render(arg ? :argfile_case_arg : :argfile_case_no_arg).indent 2
>   ;;
>

if negatable
  > {{ negated_long }})
  >   unset "args[{{ name }}]"
  >   ;;
  >
end
